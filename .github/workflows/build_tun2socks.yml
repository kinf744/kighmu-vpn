name: Build tun2socks armeabi-v7a

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: sudo apt-get install -y cmake ninja-build

      - name: Setup Android NDK
        run: |
          wget -q https://dl.google.com/android/repository/android-ndk-r26d-linux.zip
          unzip -q android-ndk-r26d-linux.zip
          echo "NDK_HOME=$PWD/android-ndk-r26d" >> $GITHUB_ENV

      - name: Clone BadVPN
        run: git clone --depth=1 https://github.com/ambrop72/badvpn.git

      - name: Build tun2socks armeabi-v7a
        run: |
          cd badvpn
          mkdir build && cd build
          cmake .. \
            -DCMAKE_TOOLCHAIN_FILE=$NDK_HOME/build/cmake/android.toolchain.cmake \
            -DANDROID_ABI=armeabi-v7a \
            -DANDROID_PLATFORM=android-21 \
            -DANDROID_STL=c++_static \
            -DBUILD_NOTHING_BY_DEFAULT=1 \
            -DBUILD_TUN2SOCKS=1 \
            -DCMAKE_BUILD_TYPE=Release
          make -j$(nproc)
          ls -lh tun2socks/
          cp tun2socks/tun2socks ../../libtun2socks.so

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: libtun2socks-armeabi-v7a
          path: libtun2socks.so
